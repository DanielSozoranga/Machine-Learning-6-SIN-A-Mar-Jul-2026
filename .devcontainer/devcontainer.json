{
  "name": "DS Final ML — Linear & Logistic Regression",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "ms-toolsai.jupyter-renderers",
        "ms-toolsai.jupyter-keymap",
        "charliermarsh.ruff",
        "tamasfe.even-better-toml"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/workspace/ds/bin/python",
        "jupyter.kernels.filter": [
          { "path": "/workspace/ds/bin/python", "type": "pythonEnvironment" }
        ],
        "python.terminal.activateEnvironment": true,
        "workbench.editorAssociations": {
          "*.ipynb": "jupyter-notebook",
          "README.md": "vscode.markdown.preview.editor"
        },
        "[python]": {
          "editor.defaultFormatter": "charliermarsh.ruff",
          "editor.formatOnSave": true
        },
        "files.exclude": {
          "**/__pycache__": true,
          "**/.ipynb_checkpoints": true,
          "**/.ruff_cache": true
        }
      }
    },
    "codespaces": {
      "openFiles": []
    }
  },
  "postCreateCommand": "echo '[ -f /workspace/ds/bin/activate ] && source /workspace/ds/bin/activate' >> /root/.bashrc && echo '' && echo '════════════════════════════════════════════════════════════' && echo '  ✅ DS Final ML — DevContainer listo' && echo '════════════════════════════════════════════════════════════' && echo '  • Venv:    /workspace/ds  (auto-activado en cada terminal)' && echo '  • Kernel:  Python 3.12 (DS Final ML)' && echo '' && echo '  → Abre notebooks/main.ipynb desde el Explorer y Run All' && echo '════════════════════════════════════════════════════════════' && echo ''",
  "remoteUser": "root",
  "forwardPorts": [8888]
}